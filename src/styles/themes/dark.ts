import base from './base';
// eslint-disable-next-line import/no-cycle
import { extend } from './utils';

export default extend(base, {
  backgroundPrimary: '#fef9e5',
  backgroundSecondary: '#d3b022',
  backgroundInverse: '#d3b022',
  textSecondary: '#00c4b3',
  textPrimary: '#d3b022',
});
